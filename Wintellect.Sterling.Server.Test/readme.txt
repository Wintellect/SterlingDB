
To run unit tests against the Azure Table Storage driver, set the AZURE_DRIVER conditional compile directive in the test project settings.

You'll also need to install the Azure SDK and manually start the storage emulator.

Find the Azure SDK here:  http://www.windowsazure.com/en-us/downloads/
